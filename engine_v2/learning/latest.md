# NiakVIO Brain — Learning Lab

Daily sandbox execution and sanitized cross-day memory. Nothing in this report publishes a provider or manifest to production automatically.

Generated: 2026-08-29T17:07:11.514Z

Learned skills observed: **1**
Negative-memory entries: **151**
Historical high/critical unresolved: **0**
Native repair-priority providers: **57**
Native reader failures: **2379**
Targeted learning Lab: **multi_provider** — fullanime / undefined

## Highest-priority proposals

### 1. native_reader_failure_class — critical

Failure class: `media_extraction_gap`
Official native readers observed 2210 media_extraction_gap failure(s). Prefer reader-causal repair before generic provider mutation.

Candidate composition: `capture-media-network → inspect-player-javascript`

### 2. native_reader_failure_class — critical

Failure class: `playback_timeout`
Official native readers observed 147 playback_timeout failure(s). Prefer reader-causal repair before generic provider mutation.

Candidate composition: `diagnose-native-reader-timeout`

### 3. native_reader_failure_class — critical

Failure class: `short_media`
Official native readers observed 22 short_media failure(s). Prefer reader-causal repair before generic provider mutation.

Candidate composition: `reject-short-or-preview-media`

### 4. native_reader_provider_target — critical

Provider: `voiranime-homes`
Failure class: `short_media`
Native player failure for this provider is causally classified as short_media; retest the same provider/fixture/reader before broad repair.

### 5. native_reader_provider_target — critical

Provider: `animoflix`
Failure class: `short_media`
Native player failure for this provider is causally classified as short_media; retest the same provider/fixture/reader before broad repair.

### 6. native_reader_repeated_signature — critical

Provider: `hindmoviez`
Failure class: `playback_timeout`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 7. native_reader_repeated_signature — critical

Provider: `animepahe`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 8. native_reader_repeated_signature — critical

Provider: `animesalt`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 9. native_reader_repeated_signature — critical

Provider: `movix`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 10. native_reader_repeated_signature — critical

Provider: `papadustream`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 11. native_reader_repeated_signature — critical

Provider: `streamzo`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 12. native_reader_repeated_signature — critical

Provider: `purstream`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 13. native_reader_repeated_signature — critical

Provider: `dulourd`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 14. native_reader_repeated_signature — critical

Provider: `allmovieland`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 15. native_reader_repeated_signature — critical

Provider: `cinemacity`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 16. native_reader_repeated_signature — critical

Provider: `cinemm`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 17. native_reader_repeated_signature — critical

Provider: `desiflix`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 18. native_reader_repeated_signature — critical

Provider: `dooflix`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 19. native_reader_repeated_signature — critical

Provider: `flemmix`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 20. native_reader_repeated_signature — critical

Provider: `movies4u`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 21. native_reader_repeated_signature — critical

Provider: `moviesdrive`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 22. native_reader_repeated_signature — critical

Provider: `moviesmod`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 23. native_reader_repeated_signature — critical

Provider: `streamflix`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 24. native_reader_repeated_signature — critical

Provider: `vidlink`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 25. native_reader_repeated_signature — critical

Provider: `wookafr`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 26. native_reader_repeated_signature — critical

Provider: `yflix`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 27. native_reader_repeated_signature — critical

Provider: `zinkmovies`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 28. native_reader_repeated_signature — critical

Provider: `4khdhub`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 29. native_reader_repeated_signature — critical

Provider: `persianstremio`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 30. native_reader_repeated_signature — critical

Provider: `animezey`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 31. native_reader_repeated_signature — critical

Provider: `vidfast`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 32. native_reader_repeated_signature — critical

Provider: `movieblast`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 33. native_reader_repeated_signature — critical

Provider: `animesultra`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 34. native_reader_repeated_signature — critical

Provider: `animevostfr`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 35. native_reader_repeated_signature — critical

Provider: `mugiwarastream`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 36. native_reader_repeated_signature — critical

Provider: `animesama-co`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 37. native_reader_repeated_signature — critical

Provider: `castle`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 38. native_reader_repeated_signature — critical

Provider: `cineby`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 39. native_reader_repeated_signature — critical

Provider: `hdhub4u`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 40. native_reader_repeated_signature — critical

Provider: `hindmoviez`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 41. native_reader_repeated_signature — critical

Provider: `vegamovies`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 42. native_reader_repeated_signature — critical

Provider: `videasy`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 43. native_reader_repeated_signature — critical

Provider: `voiranime`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 44. native_reader_repeated_signature — critical

Provider: `voiranime-rip`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 45. native_reader_repeated_signature — critical

Provider: `vostfree`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 46. native_reader_repeated_signature — critical

Provider: `anime-sama`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 47. native_reader_repeated_signature — critical

Provider: `french-manga`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 48. native_reader_repeated_signature — critical

Provider: `nakios`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 49. native_reader_repeated_signature — critical

Provider: `anikototv`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 50. native_reader_repeated_signature — critical

Provider: `animetsu`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 51. native_reader_repeated_signature — critical

Provider: `anizone`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 52. native_reader_repeated_signature — critical

Provider: `hianime`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 53. native_reader_repeated_signature — critical

Provider: `anidb`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 54. native_reader_repeated_signature — critical

Provider: `goated`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 55. native_reader_repeated_signature — critical

Provider: `hdghartv`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 56. native_reader_repeated_signature — critical

Provider: `kisskh`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 57. native_reader_repeated_signature — critical

Provider: `peachify`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 58. native_reader_repeated_signature — critical

Provider: `playimdb`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 59. native_reader_repeated_signature — critical

Provider: `vidrock`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 60. native_reader_repeated_signature — critical

Provider: `kurage`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 61. native_reader_repeated_signature — critical

Provider: `movieshunt`
Failure class: `media_extraction_gap`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 62. native_reader_repeated_signature — critical

Provider: `movieshunt`
Failure class: `playback_timeout`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 63. native_reader_repeated_signature — critical

Provider: `hdhub4u`
Failure class: `playback_timeout`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 64. native_reader_repeated_signature — critical

Provider: `french-manga`
Failure class: `short_media`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 65. native_reader_repeated_signature — critical

Provider: `videasy`
Failure class: `playback_timeout`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 66. native_reader_repeated_signature — critical

Provider: `hdhub4u`
Failure class: `short_media`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 67. native_reader_repeated_signature — critical

Provider: `purstream`
Failure class: `playback_timeout`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 68. native_reader_repeated_signature — critical

Provider: `animezey`
Failure class: `playback_timeout`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 69. native_reader_repeated_signature — critical

Provider: `voiranime-homes`
Failure class: `short_media`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 70. native_reader_repeated_signature — critical

Provider: `persianstremio`
Failure class: `playback_timeout`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 71. native_reader_repeated_signature — critical

Provider: `animoflix`
Failure class: `short_media`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 72. native_reader_repeated_signature — critical

Provider: `vegamovies`
Failure class: `playback_timeout`
The same native-reader causal failure repeated. Stop generic retries; use the matching Brain v4 recipe and require a fresh reader proof before acceptance.

### 73. skill_candidate — high

Failure class: `search_gap`
Repeated unresolved failure class without a trusted reusable skill.

Candidate composition: `rediscover-search-route → repair-search-parser`

### 74. skill_candidate — high

Failure class: `transport_blocked`
Repeated unresolved failure class without a trusted reusable skill.

Candidate composition: `bootstrap-session → alternate-official-route`

### 75. skill_candidate — high

Failure class: `unknown_failure`
Repeated unresolved failure class without a trusted reusable skill.

Candidate composition: `collect-missing-evidence`

### 76. avoid_failed_profile — high

Provider: `animesalt`
Failure class: `search_gap`
Profile: `adaptive_runtime_recovery`
The same sandbox profile failed 3 consecutive time(s) for this provider/signature. Collect different evidence or use another repair hypothesis before retrying it.

### 77. native_reader_provider_target — high

Provider: `hindmoviez`
Failure class: `playback_timeout`
Native player failure for this provider is causally classified as playback_timeout; retest the same provider/fixture/reader before broad repair.

### 78. native_reader_provider_target — high

Provider: `animepahe`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 79. native_reader_provider_target — high

Provider: `animesalt`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 80. native_reader_provider_target — high

Provider: `movix`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 81. native_reader_provider_target — high

Provider: `papadustream`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 82. native_reader_provider_target — high

Provider: `streamzo`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 83. native_reader_provider_target — high

Provider: `purstream`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 84. native_reader_provider_target — high

Provider: `dulourd`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 85. native_reader_provider_target — high

Provider: `hdhub4u`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 86. native_reader_provider_target — high

Provider: `persianstremio`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 87. native_reader_provider_target — high

Provider: `allmovieland`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 88. native_reader_provider_target — high

Provider: `cinemacity`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 89. native_reader_provider_target — high

Provider: `cinemm`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 90. native_reader_provider_target — high

Provider: `desiflix`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 91. native_reader_provider_target — high

Provider: `dooflix`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 92. native_reader_provider_target — high

Provider: `flemmix`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 93. native_reader_provider_target — high

Provider: `movies4u`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 94. native_reader_provider_target — high

Provider: `moviesdrive`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 95. native_reader_provider_target — high

Provider: `moviesmod`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 96. native_reader_provider_target — high

Provider: `streamflix`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 97. native_reader_provider_target — high

Provider: `vidlink`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 98. native_reader_provider_target — high

Provider: `wookafr`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 99. native_reader_provider_target — high

Provider: `yflix`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

### 100. native_reader_provider_target — high

Provider: `zinkmovies`
Failure class: `media_extraction_gap`
Native player failure for this provider is causally classified as media_extraction_gap; retest the same provider/fixture/reader before broad repair.

## Privacy

No raw URLs, tokens, header values, cookies, private notes or spreadsheet text are copied into persistent Brain learning state.
